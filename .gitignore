# Java
backend/target/
*.class
*.jar
*.war

# Maven
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo

# Node
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
# Production secrets, written on the server. Never committed — .env.* was not covered by the
# patterns above, so a `git add .` on the instance would have committed live credentials.
.env.prod
.env.production
.env.staging

# OS
.DS_Store
Thumbs.db

# Uploads
uploads/

# Official BodyParts3D source archives/tables are large provenance inputs, not app assets.
.anatomy-source-cache/

# Logs
*.log

# Wrangler
.wrangler/
.dev.vars

# SSH keys — never commit a private key
*.pem
*.ppk
*.key
