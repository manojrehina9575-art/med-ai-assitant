name: Production CI/CD Pipeline

on:
  push:
    branches: [ main, staging ]
  pull_request:
    branches: [ main ]

jobs:
  backend-build-and-test:
    name: Backend Build, Lint & Test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: medai
          POSTGRES_USER: medai
          POSTGRES_PASSWORD: medai_secret
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run Flyway & Unit Tests
        # Points the tests at the service container above. Without these the suite ignored it and
        # started a second database through Testcontainers — two Postgres instances per run, one of
        # them unused.
        env:
          MEDAI_TEST_JDBC_URL: jdbc:postgresql://localhost:5432/medai
          MEDAI_TEST_DB_USER: medai
          MEDAI_TEST_DB_PASSWORD: medai_secret
        # `verify`, not `test`: that is the phase the JaCoCo coverage ratchet binds to, so
        # running `test` would produce the report and never check it.
        run: |
          cd backend
          mvn clean verify --batch-mode

      - name: Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: backend/target/site/jacoco/
          if-no-files-found: warn

  frontend-build-and-test:
    name: Frontend Build & Typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install & Build Frontend
        run: |
          cd frontend
          npm ci
          npm run build

  container-security-scan:
    name: Security & Vulnerability Scan
    needs: [backend-build-and-test, frontend-build-and-test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          format: 'table'
          severity: 'CRITICAL,HIGH'

  deploy-staging:
    name: Deploy to Staging (Kubernetes)
    needs: [container-security-scan]
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy Helm Chart to Staging
        run: |
          echo "Deploying med-ai-assistant to staging cluster via Helm..."
          helm upgrade --install medai-staging ./helm/med-ai-assistant -n medai-staging --create-namespace

  deploy-production:
    name: Deploy to Production (Kubernetes)
    needs: [container-security-scan]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy Helm Chart to Production
        run: |
          echo "Deploying med-ai-assistant to production cluster via Helm..."
          helm upgrade --install medai-prod ./helm/med-ai-assistant -n medai-prod --create-namespace
